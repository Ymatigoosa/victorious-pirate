import filepicker from 'filepicker-js';

filepicker.setKey('APNVn3FlR6yU5HyVrtxIgz');
const initialState = filepicker;

function reducer(state = initialState, action) {
      return state;
}

export default reducer;
